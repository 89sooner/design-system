# Session: 2026-07-12 — WP-018~022 docs site recovery after forced compaction

## Goal

Recover the work lost from conversational context by reading the current changes, capture what WP-018~022 implemented and what remains red, then rebuild the handoff pack.

## Current state

- The uncommitted tree implements WP-018 through WP-022 as a Vite/React static docs site.
- Reverified green: root build, root typecheck, docs Playwright 15/15, token lint, contrast 80/80, SRS/PRD validator report and strict.
- Not green: root Vitest has three tokens failures caused by the current tokens project jsdom environment; ESLint has five repository errors, two introduced in docs and three pre-existing in React contract tests.
- Delivery summary marks WP-018~022 done, but their individual DoD checkboxes remain unchecked. FR-DOC-001 also remains partial for a reason that cites now-completed WP-019~022.

## Decisions

- Do not start WP-023 until the root test regression and delivery-document drift are resolved.
- Treat generated token/catalog/contrast artifacts as inputs produced by package builds, never as hand-edited source.
- Keep theme selection on the root `data-cdt-theme` attribute, with an inline pre-React first-paint script and guarded localStorage access.
- Keep docs as a consumer of public package entry points; no source-relative package imports.
- Component catalog coverage is build-fatal: every declaration export recognized as public must have a live preview case.

## Changed files

- Replaced docs tsup placeholder: deleted `src/main.ts`, `src/main.test.ts`, `tsup.config.ts`.
- Added Vite/React app: `index.html`, `vite.config.ts`, `src/main.tsx`, `src/App.tsx`, `src/theme.ts`, `src/docs.css`.
- Added generated-data consumers: `src/foundations.ts`, `src/catalog.tsx`, `src/token-reference.tsx`, `src/guides.tsx`.
- Added build scripts: `scripts/check-foundation-tokens.mjs`, `build-component-catalog.mjs`, `build-contrast-report.mjs`.
- Added Playwright config and 15 tests across `e2e/shell`, `foundations`, `catalog`, `tokens`, and `guides`.
- Updated docs package dependencies/scripts, lockfile, generated ignores, delivery status/traceability, and root Vitest config.

## Commands

- PASS: `pnpm build` — tokens 318, CSS index gzip 6,812 B, docs Vite build.
- PASS: `pnpm typecheck`.
- PASS: `pnpm --filter docs test:e2e` — 15/15 when browser/local server can run.
- PASS: `pnpm lint:tokens` — 38 files, 0 violations.
- PASS: `pnpm check:contrast` — 80/80.
- PASS: planning validator `--report` and `--strict`.
- FAIL: `pnpm test` — `The URL must be of scheme file` and `/src/tokens.ts` ENOENT under tokens jsdom.
- FAIL: `eslint .` — docs catalog unused callback args (2), pre-existing React contract issues (3).
- ENV-ONLY FAIL: sandboxed Playwright could not start preview; unsandboxed rerun passed 15/15.

## Next steps

1. Restore a node environment for tokens tests unless a concrete docs-unit-test requirement proves otherwise, then rerun root Vitest.
2. Fix the two new docs lint failures; decide and record handling for the three pre-existing React lint failures.
3. Close WP-018~022 DoD boxes from evidence and reassess FR-DOC-001 status AC by AC.
4. Rerun all gates, then implement WP-023 AppShell/NavList/TopBar.

## Risks/gotchas

- A green docs suite does not imply the root test suite is green.
- Vite emits a >500 kB minified chunk warning. It is not currently a WP-018~022 gate; avoid scope-creep optimization before WP-025/WP-028.
- Build-generated `src/generated/*`, `dist/`, and token generated TS are ignored and must not be committed or edited.
- Playwright needs localhost/browser permissions unavailable in some sandboxes.

## References

- WP-018~023; FR-DOC-001~007; FR-CMP-009; API-DOC-001; API-CMP-009.
