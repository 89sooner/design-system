#hidden
# aci:v1 id=f54408e src=agent-context/todos.md
@kv sha256=efd7d6b957f126dd5907bf6deab9a131a697ca5ffa3f84e8627449a559352729 bytes=3378 lines=63 title=다음-작업-/-미해결-항목
@sig agent-context/todos.md;15/15;agent-context/sessions/2026-07-12-wp018-022-docs-site.md;error/info;conductor/react;click/Escape;focus/keyboard;id/focus;claude/skills/build-srs-prd-env/scripts/validate_srs_prd_env.py;browser/localhost;FilterBar/Chip;packages/tokens/src/tokens.ts;src/breakpoints.ts;src/generated/;Playwright;delivery;context;sessions;wp018;catalog;ESLint;React;DoD;DOC
@h1 다음 작업 / 미해결 항목
@h2 현재 시작 지점
@path WP-018~022 구현은 작업 트리에 있고 docs 빌드·타입검사·Playwright 15/15가 통과한다. 그러나 루트 test와 lint가 red이고 delivery 문서 동기화가 덜 끝났으므로 WP-023을 바로 시작하지 않는다.
@code lang=text sha=bb74ae0dec63 lines=6 kept=6
|1. agent-context/sessions/2026-07-12-wp018-022-docs-site.md를 읽는다
|2. vitest.config.ts의 tokens jsdom 변경을 검토하고 pnpm test를 복구한다
|3. docs catalog ESLint 2건과 기존 React lint 3건의 범위를 확인한다
|4. WP-018~022 DoD 체크박스와 FR-DOC-001 상태를 AC별 증거로 동기화한다
|5. 전체 게이트를 다시 통과시킨다
|6. 그 뒤 WP-023 셸 컴포넌트군을 시작한다
@h2 P0 — handoff 시점의 red 상태 해소
@b pnpm test: 3 failures. vitest.config.ts가 tokens project를 node → jsdom으로 바꾼 것이 원인이다. file-URL 기반 테스트 3곳이 깨진다.
@risk eslint .: 5 errors. 새 docs 2건(catalog.tsx unused error/info) + 기존 React contract test 3건.
@b 가장 작은 test 복구 후보는 tokens project의 environment: "node", include: ["src//*.test.ts"] 복원이다. docs E2E는 Playwright라 이 설정에 의존하지 않는다.
@h2 P1 — WP-018~022 문서 마감
@path conductor_work_packages.md: summary는 done이지만 WP-018~022의 모든 DoD가 [ ]다. 테스트/코드 증거를 확인해 동기화한다.
@path conductor_implementation_traceability.md: FR-DOC-001이 “WP-019~022 미완료”를 전제로 부분인데 그 WP들은 같은 파일에서 done이다. AC-2 포함 전체 요구를 읽고 상태를 재평가한다.
@path docs 빌드의 507.34 kB chunk warning은 기록만 한다. WP-025 size gate와 WP-028 Lighthouse 전에는 임의 최적화하지 않는다.
@h2 P2 — 다음 구현 WP
@path WP-023 셸 컴포넌트군
@path C-070 AppShell, C-071 NavList, C-072 TopBar.
@path NavList.renderLink로 링크 렌더를 소비자에게 위임한다. @conductor/react에 React Router를 추가하지 않는다.
@path 800px 미만 off-canvas는 overlay click/Escape로 닫히며 Radix가 focus/keyboard behavior를 소유한다.
@path AppShell은 skip link와 main id/focus 연결을 제공한다.
@b public exports, publicComponents registry, SSR renderer, shared contract suite를 함께 갱신한다.
@h2 검증 순서
@code lang=text sha=d17a38e7c7e9 lines=9 kept=9
|pnpm build
|pnpm typecheck
|pnpm test
|pnpm lint
|pnpm lint:tokens
|pnpm check:contrast
|pnpm --filter docs test:e2e
|python3 ~/.claude/skills/build-srs-prd-env/scripts/validate_srs_prd_env.py --root . --report
|python3 ~/.claude/skills/build-srs-prd-env/scripts/validate_srs_prd_env.py --root . --strict
@path Playwright는 제한된 sandbox에서 preview webServer가 뜨지 않을 수 있다. 같은 명령이 browser/localhost 권한을 가진 실행에서는 15/15 통과했다.
@h2 열린 결정
@todo OD-003 FilterBar/Chip의 v1 포함 여부만 open이며 Must FR을 차단하지 않는다. FR이 없으므로 구현하지 않는다.
@h2 계속 지킬 것
@b srs_final.md는 baseline v1.2. 요구사항 의미 변경은 CR 선등록 후 cascade한다.
@path 생성 파일 packages/tokens/src/tokens.ts, src/breakpoints.ts, docs src/generated/*를 직접 편집하지 않는다.
@b 코드/문서 불일치는 DEV → CR 절차 없이 조용히 바꾸지 않는다.
@path 도메인 컴포넌트는 이식하지 않는다(F-X-009).
