#hidden
# aci:v1 id=f0a7a40 src=agent-context/sessions/2026-07-18-pages-routing-visual-upgrade.md
@kv sha256=3599e5d30939a97d8b65cb9fc955a854af9b0211da461f6a1d623c92e7fb3d59 bytes=4785 lines=67 title=Session-2026-07-18-visual-language-upgrade-and-Pages-route-recovery
@sig agent-context/sessions/2026-07-18-pages-routing-visual-upgrade.md;home/roqkf/agent-ai-platform;indigo/teal;dark/light;localhost/Chromium;refresh/pack;rewrite/404;packages/tokens/src/palette.dark.ts;AppShell/NavList;packages/css/src/components.css;test/bundle.test.ts;gradient/glass;apps/docs/src/main.tsx;scripts/build-pages-fallback.mjs;github/workflows/deploy-docs.yml;apps/docs/e2e/routes.ts;E2E/visual;docs/00_governance/change_control.md;token/flow/component;tmp/conductor-corepack;80/80;claude/skills/build-srs-prd-env/scripts/validate_srs_prd_env.py;stage/commit/push;89sooner.github.io/design-system/
@h1 Session: 2026-07-18 — visual language upgrade and Pages route recovery
@h2 Goal
@path 사용자는 /home/roqkf/agent-ai-platform의 더 정제된 시각 언어(특히 gradient)를 Conductor에 반영하고, GitHub Pages에서 overview 외 경로가 빈 화면이 되는 문제를 고쳐 달라고 요청했다.
@h2 Current state
@path AppShell은 원본 app.css의 indigo/teal ambient radial glow를 dark/light 공용 component-token 계약으로 소비한다.
@b NavList는 glass surface와 backdrop blur를 사용한다.
@b docs는 HashRouter를 사용하며, Pages의 legacy path 404 응답은 /#/...로 변환된다. SPA rewrite 지원에 의존하지 않는다.
@b build:pages가 배포용 404.html을 생성하고 deploy workflow가 이를 사용한다.
@path 구현·문서·route fixture를 포함한 로컬 정적 게이트는 통과했다. 실제 Pages 클릭 검증은 이 sandbox의 localhost/Chromium 제한 때문에 수행하지 못했다.
@path 사용자 요청에 따라 handoff를 refresh/pack했지만, 관리 sandbox가 .git을 read-only로 마운트해 git add가 index lock 생성에서 실패했다. 이 세션에서는 스테이징·커밋·push가 수행되지 않았다.
@h2 Decisions
@path 배경 장식은 layout CSS가 직접 소유하지 않고 appShell.background component token으로 둔다. CSS 계층 계약(FR-CSS-003)을 보존한다.
@b 기존 CSS가 참조하지만 정의되지 않았던 surface.tint.1을 포함해 surface.tint.1/.2를 decorative semantic token으로 두 테마에 추가한다. 텍스트 대비 쌍에는 넣지 않는다.
@path GitHub Pages의 route rewrite/404 동작을 전제로 하지 않는다. HashRouter를 canonical route로 하고, 이미 공유된 path URL은 generated 404 fallback으로 호환한다.
@path 이 변경은 CR-031로 기록했다. 공개 Pages와 실제 브라우저 검증이 남아 있으므로 CR 상태는 open이다.
@h2 Changed files
@path packages/tokens/src/palette.dark.ts, palette.light.ts, components.ts — tint semantic tokens와 AppShell/NavList component values.
@path packages/css/src/components.css, test/bundle.test.ts — AppShell gradient/glass nav 및 public CSS contract.
@path apps/docs/src/main.tsx, docs.css, scripts/build-pages-fallback.mjs, package.json — hash router, transparent docs shell, Pages fallback build.
@path .github/workflows/deploy-docs.yml — build:pages artifact deploy.
@path apps/docs/e2e/routes.ts 및 E2E/visual specs — canonical hash route fixture.
@path docs/00_governance/change_control.md와 derived UI specs — CR-031, token/flow/component contract.
@h2 Commands
@code lang=bash sha=e214bebbb77b lines=9 kept=9
|# Managed sandbox requires a writable Corepack cache.
|COREPACK_HOME=/tmp/conductor-corepack pnpm build
|COREPACK_HOME=/tmp/conductor-corepack pnpm typecheck
|COREPACK_HOME=/tmp/conductor-corepack pnpm test       # 490 passed
|COREPACK_HOME=/tmp/conductor-corepack pnpm lint:tokens
|COREPACK_HOME=/tmp/conductor-corepack pnpm check:contrast  # 80/80
|DOCS_BASE=/design-system/ COREPACK_HOME=/tmp/conductor-corepack pnpm --filter docs run build:pages
|python3 ~/.claude/skills/build-srs-prd-env/scripts/validate_srs_prd_env.py --root . --strict
|git -c core.fileMode=false diff --check
@p Failed only because of environment: local server bind returned EPERM; Chromium launch failed at sandbox_host_linux; live web lookup was unavailable. Do not treat these as product failures.
@h2 Next steps
@path .git 쓰기 권한이 있는 환경에서 현재 작업 트리를 stage/commit/push하고, Pages workflow를 배포한다.
@path Verify https://89sooner.github.io/design-system/#/components and a legacy direct path such as /design-system/components in a real browser.
@path Run visual/a11y browser gates in GitHub Actions or a browser-capable environment; intentional gradient changes may require visual baseline review.
@path Close CR-031 only after the Pages/browser result is recorded.
@h2 Risks/gotchas
@b Root overview is prerendered; non-overview pages need client routing. A static root can appear healthy even when client navigation is broken.
@path Generated packages/tokens/src/tokens.ts is ignored. Never edit it; build regenerates it.
@todo Hash routes must be used by Playwright fixtures. A path-only page.goto('/components') no longer exercises the canonical URL.
@todo Existing dirty agent-context changes came from the public-release handoff and should remain included when rebuilding the pack; do not discard them.
@cmd 이 sandbox에서 git add는 .git/index.lock: Read-only file system으로 실패한다. 파일 수정 검증과 Git index mutation을 구분한다.
@h2 References
@path CR-031 / DEV-024
@path Original visual source: /home/roqkf/agent-ai-platform/packages/web/src/styles/app.css:14-19
@path Public docs: https://89sooner.github.io/design-system/
